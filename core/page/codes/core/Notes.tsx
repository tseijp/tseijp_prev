import React, {FC} from 'react'
import {Notes as Target, Card} from '../../../src'

export const Notes:FC = () => {
    return (
        <Target>
            <Card>
            </Card>
        </Target>
    )
}
